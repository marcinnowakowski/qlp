(ns qlp.core)

(defn what
[x]
(do(println "what(" x ")")
 x))
  
(defn _if
  [x]
  (do(println "if(" x ")")
   x))

(defn plq
  [x]
  (do(println "plq(" x ")")
   x))
  
(defn _is
  [x]
  (do(println "is(" x ")")
   x))
  
(defn better
  [x]
  (do(println "better(" x ")")
   x))
  
(defn _then
  [x]
  (do(println "then(" x ")")
   x))

(defn qlp
  [x]
  (do(println "qlp(" x ")")
   x))

(defn _?
  [x]
  (do(println "?(" x ")")
   x))

(defn i
  [x]
  (do(println "I(" x ")")
   x))

(defn dont
  [x]
  (do(println "don't(" x ")")
   x))

(defn know
  [x]
  (do(println "know(" x ")")
   x))